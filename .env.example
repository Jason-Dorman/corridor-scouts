# Database (Neon/Supabase) or local Docker (see docker-compose.yml)
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/corridor_scouts"

# Redis (Upstash) or local Docker (see docker-compose.yml)
REDIS_URL="redis://localhost:6379"

# RPC Providers (Alchemy — https://www.alchemy.com)
ALCHEMY_API_KEY="your_alchemy_api_key_here"

# Optional: WebSocket URL for real-time frontend updates
NEXT_PUBLIC_WS_URL="wss://your-app.vercel.app"
